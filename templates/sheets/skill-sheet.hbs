<form class="{{cssClass}}" autocomplete="off">
    <div class="item-sheet-header">
        <h1>
            <input
                name="name"
                type="text"
                value="{{item.name}}"
                placeholder="{{localize "cd10.sheet.name"}}"
            />
        </h1>
    </div>
    <hr>
    <div class="item-sheet-main-content flex-row flex-evenly">
        <img
            src="{{item.img}}"
            data-edit="img"
            title="{{item.name}}"
        />
        <div class="generic-info-box cd10-card-detail pad-8">
            <div class="editor-box">
                {{editor
                    content=data.description.value
                    target="system.description.value"
                    button=true
                    owner=owner
                    editable=editable
                }}
            </div>
        </div>
    </div>
    <div class="weapon-and-armor-stats cd10-card-detail">
        {{> "systems/cd10/templates/partials/item-sheet-components/small-skill-stats.hbs" this}}
    </div>
</form>